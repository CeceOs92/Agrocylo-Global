{
  "extends": ["next/core-web-vitals"],
  "rules": {
    "no-restricted-syntax": [
      "error",
      {
        "selector": "BinaryExpression[operator='/'] > Literal[value=10000000]",
        "message": "Do not divide chain amounts by 1e7 — Number/double math loses precision on i128 stroop values. Use formatStroops/formatStroopsForDisplay from @/lib/validation."
      },
      {
        "selector": "BinaryExpression[operator='/'][right.raw=/^1[eE]7$/]",
        "message": "Do not divide chain amounts by 1e7 — use formatStroops/formatStroopsForDisplay from @/lib/validation."
      }
    ]
  },
  "overrides": [
    {
      "files": ["src/lib/validation.ts"],
      "rules": { "no-restricted-syntax": "off" }
    }
  ]
}
