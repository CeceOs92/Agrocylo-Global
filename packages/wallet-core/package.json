{
  "name": "@agrocylo/wallet-core",
  "version": "0.1.0",
  "private": true,
  "description": "Shared Stellar/Freighter wallet-bridge detection, consumed by client and agro-production/client (Issue #755).",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "lint": "tsc --noEmit",
    "build": "tsc --noEmit",
    "test": "echo \"no tests yet\" && exit 0"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
